# Core dependencies
pyyaml>=6.0
numpy>=1.24.0
scikit-learn>=1.3.0
requests>=2.31.0

# OpenRouter SDK for multi-model routing
openrouter>=0.1.0

# Shodan for external exposure scoring
shodan>=1.31.0

# Optional: ML Detector (𝓜) — CodeBERT-based vulnerability classification
# transformers>=4.35.0
# torch>=2.0.0

# Optional: YARA rule matching for binary targets
# yara-python>=4.3.0

# Optional: Vector DB for cross-run context persistence
# chromadb>=0.4.0

# Optional: OpenAI Agents SDK backend
# openai-agents>=0.1.0
